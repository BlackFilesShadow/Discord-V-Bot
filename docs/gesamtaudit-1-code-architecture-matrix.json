{
  "schemaVersion": 3,
  "stage": 60,
  "basedOnMainSha": "0e4972924e06f837813555b41df636e7ad06453e",
  "scope": "Gesamtaudit 1: code & architecture — layering, v2 auth stack, architecture gate corpus, AI tool path, rebound to current main after Stage 59 merge.",
  "status": "PARTIAL",
  "contracts": {
    "gates": "tests/security/*Architecture*.test.ts form permanent drift gates (>=40)",
    "v2": "all dashboard API under /api/v2 with requireAuth + idempotency",
    "aiTools": "toolRuntime remains sole mutation path for AI tools",
    "scan": "scripts/gesamtaudit-scan.mjs",
    "currentMainBinding": "Stage-60 evidence must bind to exact current main SHA and must not reuse historical SHA evidence"
  },
  "cases": [
    { "id": "arch-gate-corpus-ge-40", "status": "runtime-verified" },
    { "id": "v2-requireAuth-idempotency", "status": "runtime-verified" },
    { "id": "ai-tool-runtime-security-path", "status": "runtime-verified" },
    { "id": "scan-exit-zero", "status": "runtime-verified" },
    { "id": "current-main-exact-sha-binding", "status": "runtime-verified" }
  ],
  "residual": [
    "Full design-doc rewrite remains outside the executable architecture-gate proof",
    "Dynamic import graph completeness remains Stage 61 scope"
  ]
}
